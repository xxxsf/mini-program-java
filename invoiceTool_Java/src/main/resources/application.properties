spring.application.name=invoiceTool

# Server binding for cloud run
server.address=0.0.0.0
server.port=8080

# Database (default to cloud DB, fallback to local)
spring.datasource.url=${DB_URL:jdbc:mysql://sh-cynosdbmysql-grp-43um88im.sql.tencentcdb.com:25702/invoicetool?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:8GzE5x7p}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
wechat.appid=${WECHAT_APPID:wxb95ae2df41575bc3}
wechat.appsecret=${WECHAT_APPSECRET:0de2f8fa4905bc33933277b315b43bf9}
app.upload-dir=${UPLOAD_DIR:/tmp/uploads/}
